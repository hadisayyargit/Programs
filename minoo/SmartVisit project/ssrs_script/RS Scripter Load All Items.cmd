@echo off
:: ** Script generated by Reporting Services Scripter 2.0.0.17 **
:: ** Created by Jasper Smith (jas@sqldbatips.com)             **
:: ** See http://www.sqldbatips.com for help/support           **

::Script Variables
SET LOGFILE="RS Scripter Load Log.txt"
SET SCRIPTLOCATION=
SET BACKUPLOCATION=
SET REPORTSERVER=http://localhost/ReportServer
SET RS="C:\Program Files\Microsoft SQL Server\100\Tools\Binn\RS.EXE"
SET TIMEOUT=60

::Clear Log file
IF EXIST %logfile% DEL %logfile%

::Write Log Header
ECHO Reporting Services Scripter Load Log 2.0.0.17 >>%LOGFILE%
ECHO. >>%LOGFILE%
ECHO Starting Load at %DATE% %TIME% >>%LOGFILE%
ECHO SCRIPTLOCATION = %SCRIPTLOCATION% >>%LOGFILE%
ECHO REPORTSERVER   = %REPORTSERVER% >>%LOGFILE%
ECHO BACKUPLOCATION = %BACKUPLOCATION% >>%LOGFILE%
ECHO SCRIPTLEVEL    = SQL2008 >>%LOGFILE%
ECHO TIMEOUT        = %TIMEOUT% >>%LOGFILE%
ECHO RS             = %RS% >>%LOGFILE%
ECHO. >>%LOGFILE%

::Set Code Page
CHCP 1252

::Run Scripts

ECHO Running script "%SCRIPTLOCATION%SmartVisitReport\SmartVisitReport.fldr.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%SmartVisitReport\SmartVisitReport.fldr.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1
ECHO. >>%LOGFILE%

ECHO Running script "%SCRIPTLOCATION%SmartVisitReport\rptDarkhastChartByCount.rdl.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%SmartVisitReport\rptDarkhastChartByCount.rdl.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1
ECHO. >>%LOGFILE%

ECHO Running script "%SCRIPTLOCATION%SmartVisitReport\rptDarkhastChartByValue.rdl.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%SmartVisitReport\rptDarkhastChartByValue.rdl.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1
ECHO. >>%LOGFILE%

ECHO Running script "%SCRIPTLOCATION%SmartVisitReport\rptDarkhastStatistics.rdl.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%SmartVisitReport\rptDarkhastStatistics.rdl.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1
ECHO. >>%LOGFILE%

ECHO Running script "%SCRIPTLOCATION%SmartVisitReport\rptMoshtariChartByValue.rdl.rss" >>%LOGFILE%
%RS% -i "%SCRIPTLOCATION%SmartVisitReport\rptMoshtariChartByValue.rdl.rss" -s %REPORTSERVER% -l %TIMEOUT% -v BACKUPLOCATION="%BACKUPLOCATION%" >>%LOGFILE% 2>&1
ECHO. >>%LOGFILE%

ECHO. >>%LOGFILE%
ECHO Finished Load at %DATE% %TIME% >>%LOGFILE%
ECHO. >>%LOGFILE%
